function findIncrementalValue(value, options, delta) {
  var index = options.indexOf(value);
  index += delta;
  index += options.length;
  index %= options.length;
  return options[index];
}

module.exports = findIncrementalValue;
